{
  "name": "backend",
  "version": "1.0.0",
  "description": "this is backend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "heroku-postbuild": "heroku config:set NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "heroku": "^7.59.4",
    "mongoose": "^6.2.4",
    "nodemon": "^2.0.15",
    "npm-git-install": "^0.3.0"
  }
}
